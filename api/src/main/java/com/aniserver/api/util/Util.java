package com.aniserver.api.util;

import com.aniserver.api.util.utils.CodeUtil;
import com.aniserver.api.util.utils.CryptionUtil;
import com.aniserver.api.util.utils.FileUtil;
import com.aniserver.api.util.utils.HttpUtil;

public class Util {
    public static FileUtil file = new FileUtil();
    public static HttpUtil http = new HttpUtil();
    public static CodeUtil code = new CodeUtil();
    public static CryptionUtil cryp = new CryptionUtil();
}
